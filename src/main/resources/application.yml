spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/schedule_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  jackson:
    time-zone: Europe/Moscow

server:
  port: 8082